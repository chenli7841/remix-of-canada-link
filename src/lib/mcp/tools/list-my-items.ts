import { defineTool } from "@lovable.dev/mcp-js";
import { z } from "zod";
import { isPermissionError, permissionDeniedResult, queryFailedResult, supabaseForUser, unauthenticatedResult } from "../supabase-user";
export default defineTool({name:"list_my_items",title:"List my saved items",description:"List a small page of the signed-in customer's EPLUS My Items library. Filter by a name, SKU, or HS code when possible; ask before loading more. Values are CAD.",inputSchema:{search:z.string().max(100).optional(),offset:z.number().int().min(0).max(10000).optional(),limit:z.number().int().min(1).max(10).optional()},annotations:{readOnlyHint:true,idempotentHint:true,openWorldHint:false},handler:async({search,offset,limit},ctx)=>{if(!ctx.isAuthenticated())return unauthenticatedResult();const pageSize=limit??5,start=offset??0;let q=supabaseForUser(ctx).from("my_items").select("id,name,sku,hs_code,declared_value_cad,inner_qty,unit,mfn_rate,gst_rate,sima_involved,material,origin,brand,weight_kg,created_at,updated_at").order("created_at",{ascending:false});if(search?.trim()){const safe=search.trim().replace(/[,%()]/g," ");q=q.or(`name.ilike.%${safe}%,sku.ilike.%${safe}%,hs_code.ilike.%${safe}%`);}const{data,error}=await q.range(start,start+pageSize);if(error)return isPermissionError(error)?permissionDeniedResult():queryFailedResult();const hasMore=(data?.length??0)>pageSize;const result={currency:"CAD",items:(data??[]).slice(0,pageSize),page:{offset:start,limit:pageSize,has_more:hasMore,next_offset:hasMore?start+pageSize:null}};return{content:[{type:"text",text:JSON.stringify(result,null,2)}],structuredContent:result};}});
