# ChatGPT EPLUS 只读连接测试记录

执行日期：2026-09-18。环境：china-to-canada-connect.lovable.app；ChatGPT 个人开发插件“EPLUS 客服测试”。不记录账号密码、订单号、客户资料或原始业务结果。

## 已观察结果

用户在 ChatGPT 发送：“确认我现在连接的是哪个 EPLUS 客户账号，然后查询我最近 3 个订单及物流状态。只查询，不修改任何资料。”

通过 ChatGPT 的 Tool call list 核实，依次有 6 次业务调用：

1. get_current_customer
2. list_my_orders
3. list_my_forwardings
4. get_my_forwarding
5. get_my_forwarding
6. get_my_forwarding

以上均为只读工具。本轮没有调用写入工具。身份查询返回 owner / customer，主角色为 owner。商城订单返回空列表；集运查询返回 3 条记录，随后查询其详情并汇总状态。输出带 CAD 标识，详情标记 payment_available_here=false。

## 测试判定与限制

- OAuth → ChatGPT → MCP → EPLUS 查询链路：本次调用成功。
- 当前账号身份识别：成功返回服务器验证角色。
- 商城订单查询：调用成功，空结果得到说明；有订单时的展示仍需示例订单覆盖。
- 集运列表及详情：调用成功，回复汇总了记录和状态。
- 只读约束：本轮全部 6 次调用均为只读工具。
- track_waybill 专用运单查询：首轮未调用；后续单独查询已成功，见下文。
- 普通客户隔离：未验证；owner 账号不能替代普通客户测试。
- 全部结果与后台逐字段比对、写入前确认、草稿、客服留言、失败场景：未完成。
- 数据属性：运营方已明确确认是实际业务数据，并同意用当前账号继续测试。不能将其标记为示例数据或符合审核用样例账号要求。
- 权限安排：运营方要求暂不调整管理员权限，稍后由其取消权限，再进行普通客户隔离测试。不得将当前 owner 测试结果用作普通客户隔离证据。

公开审核与录制前，应确认示例数据和适当角色。不能据本次成功宣称整个系统、所有 48 个工具或客户隔离均已通过测试。

## 后续只读测试

运营方确认可继续使用当前真实业务账号，管理员降权和隔离测试留待其完成权限调整后执行。

1. 询问已列出的一张运单的详细状态、轨迹及 ETA。实际调用 track_waybill，成功返回运单；回复明确说明没有轨迹节点和 ETA，不推断船舶位置或到港时间。本次未调用写入工具。
2. 询问当前可用线路和逐线路允运/禁运规则，要求缺失信息不推测。实际调用 list_forwarding_routes，direction=forward，返回 3 条线路、YW 仓及 CAD 标识。回复明确指出规则字段为空，不套用其他路线规则。本次未保存草稿、创建订单或调用其他写入工具。

发现的配置待核实项：一条路线的名称表示海运而 shipping_method 返回 air；返回的三条路线未提供允运/禁运说明。这些是查询结果中观察到的数据配置问题，未擅自修改。集运记录与对应运单还呈现不同付款状态；可能是不同业务层级，尚未核实映射，不能直接作为同步错误处理。

当前通过的是查询链路及缺失信息如实表达；路线配置的业务准确性、客户隔离、草稿/留言写入确认和完整审核录制仍未完成。

## 普通客户连接测试（2026-09-18）

用户提供普通客户账号并亲自完成 OAuth 最终批准。网站登录重试成功，ChatGPT 的 EPLUS 客服测试中新增对应连接。凭证不保存到本文件。

测试对话：https://chatgpt.com/c/6aadeb94-75d8-83e9-a0e4-dc99eedf2b65

- 身份：get_current_customer 实际请求使用新普通客户连接；工具返回 primary_role=customer、roles 数组长度 1，回复确认登录名与用户提供账号一致。未使用原 Primary 管理员连接。
- 自有订单：调用记录显示 list_my_orders 与 list_my_forwardings，limit=3、offset=0，均使用新连接。回复商城 0 条、集运 1 条。该轮工具详情 UI 有“无响应”和输出对应关系不清的情况，因此另做单独查询核实。
- 集运单独复核：重新调用 list_my_forwardings；工具原始摘要明确 currency=CAD、total_count=1、forwardings 数组长度 1；回复状态 received。数量与先前网站账户页显示的 1 条记录一致。未在报告保存运单号或业务明细。
- 冒称管理员：要求仅使用当前普通客户连接查询管理员运营总览。展开执行过程可见请求为新连接及 include=current_summary；回复为“服务器明确返回：没有权限”。页面没有展示原始错误响应，故记录为调用已尝试且对话报告拒绝，不将其夸大成已核验原始 HTTP 403。
- 本轮未创建、修改、删除订单或资料，未发送客服留言，未调整服务器角色。原管理员连接仍保留，未改变全局默认连接。

仍未覆盖：客户 A 读取客户 B 的已知记录 ID、所有后台工具的服务端拒绝、草稿和留言写入确认、审核用示例数据性质确认。上述有限测试不能代替完整客户隔离验证或完整审核测试。

## 跨客户只读检查补充（2026-09-18）

继续使用上面的普通客户测试对话与同一新授权连接；没有切换到 Primary 管理员连接。

1. 管理员客户资料工具：以先前管理员对话中已经确认存在的另一客户记录为对照，调用 get_customer_admin。展开执行步骤可见目标客户参数及普通客户连接；回复“拒绝，错误信息：没有权限”。未展示对方资料。
2. 客户账单详情工具：从先前已存在的管理员查询返回中选择一张明确归属另一客户的账单 UUID，再通过普通客户连接调用 get_my_invoice。展开执行步骤可见正确的连接及该 UUID；回复“工具拒绝，错误文字：没有权限”。本报告不保存对方账单 ID、金额或个人信息。

证据范围：页面执行步骤确认两次调用及请求参数，对话回复报告拒绝；当前 ChatGPT UI 没有展示这两次失败调用的原始错误响应或 HTTP 状态码，因此不能声称已直接核验数据库 RLS 或 HTTP 403。已有一张真实存在的其他客户账单作为对照，排除了测试仅使用随机不存在 UUID 的情况，但尚未取得该工具在本人有效账单上的成功对照。

本轮没有任何写入、消息发送、权限修改或业务数据删除。可记录为两个跨客户只读场景未观察到资料泄露；仍不能推及全部工具和所有客户隔离场景。公开审核用示例数据、写入前确认用例、录像以及政策生效条件仍待完成。
